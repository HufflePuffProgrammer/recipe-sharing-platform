import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "../lib/auth-context";
import { EnvChecker } from "./components/auth/EnvChecker";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Galley - Recipe Sharing Platform",
  description: "A modern, mobile-friendly recipe sharing platform where home cooks can upload beautiful recipes, browse by tags/cuisine/diet, save favorites, rate/comment, and build collections.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          {children}
          {/* <EnvChecker /> */}
        </AuthProvider>
      </body>
    </html>
  );
}
